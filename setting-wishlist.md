## String subs

    substr(s, pos, chars)
    charat(s, pos)
    index(s, substr)            -- returns -1 on substr not found
    split(s, sep)

## Array subs

    join(a, sep)
    map(fn, a)
    grep(fn, a)
